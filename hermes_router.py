# Routes the task to the correct agent logic

def route_task(task, rules):
    # Basic prototype – real logic to follow
    return f"[Simuleret respons fra Hermes baseret på regler: {rules['build_title']}]\n{task}"